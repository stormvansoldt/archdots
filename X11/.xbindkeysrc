# Increase PulseAudio volume using the keyboard
"pactl set-sink-volume @DEFAULT_SINK@ +5000"
	XF86AudioRaiseVolume

# Decrease volume
"pactl set-sink-volume @DEFAULT_SINK@ -5000"
	XF86AudioLowerVolume
